# backend/requirements.txt

Flask==2.3.3
# Thư viện để xử lý hình ảnh
Pillow==10.0.1
# "Cầu nối" giữa Python và Tesseract OCR
pytesseract==0.3.10
# Thư viện để chuyển đổi PDF thành hình ảnh
pdf2image==1.16.3
# Cho phép frontend (chạy trên domain khác) gọi API của backend
Flask-CORS==4.0.0
# Để quản lý biến môi trường (tùy chọn nhưng nên có)
python-dotenv==1.0.0